<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lorenzo Self Publish - Carousel</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    /* Reset & Isolation */
    .lsp-carousel-wrapper * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    .lsp-carousel-wrapper {
      --purple-deep: #4A148C;
      --purple-vivid: #6A1B9A;
      --gradient-start: #5E17EB;
      --gradient-end: #450F85;
      --orange-cta: #FF914D;
      --orange-banner: #F58634;
      --yellow-highlight: #FFD700;
      --white: #FFFFFF;
      --off-white: #F9F9F9;
      --text-dark: #333333;
      
      font-family: 'Montserrat', sans-serif;
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px;
      background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-end) 100%);
      border-radius: 8px;
      position: relative;
      overflow: hidden;
    }

    /* Decorative Background Elements - Concentric Waves */
    .lsp-carousel-wrapper::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: 
        radial-gradient(circle at 20% 30%, rgba(255,145,77,0.08) 0%, transparent 30%),
        radial-gradient(circle at 80% 70%, rgba(106,27,154,0.15) 0%, transparent 35%),
        radial-gradient(circle at 50% 50%, rgba(255,255,255,0.03) 0%, transparent 50%);
      pointer-events: none;
      animation: pulse 8s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.05); opacity: 0.8; }
    }

    /* Floating Particles */
    .lsp-particles {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
    }

    .lsp-particle {
      position: absolute;
      width: 6px;
      height: 6px;
      background: rgba(255, 215, 0, 0.4);
      border-radius: 50%;
      animation: float 6s ease-in-out infinite;
    }

    .lsp-particle:nth-child(1) { top: 10%; left: 10%; animation-delay: 0s; }
    .lsp-particle:nth-child(2) { top: 20%; left: 85%; animation-delay: 1s; width: 4px; height: 4px; }
    .lsp-particle:nth-child(3) { top: 70%; left: 15%; animation-delay: 2s; background: rgba(255,145,77,0.5); }
    .lsp-particle:nth-child(4) { top: 80%; left: 80%; animation-delay: 3s; width: 8px; height: 8px; }
    .lsp-particle:nth-child(5) { top: 40%; left: 5%; animation-delay: 4s; }
    .lsp-particle:nth-child(6) { top: 60%; left: 92%; animation-delay: 5s; background: rgba(255,255,255,0.3); }

    @keyframes float {
      0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.6; }
      50% { transform: translateY(-20px) rotate(180deg); opacity: 1; }
    }

    /* Carousel Container */
    .lsp-carousel {
      position: relative;
      width: 100%;
      z-index: 1;
    }

    /* Headline */
    .lsp-headline {
      text-align: center;
      font-size: 36px;
      font-weight: 700;
      color: var(--white);
      line-height: 1.2;
      margin-bottom: 32px;
      position: relative;
      z-index: 1;
    }

    .lsp-slide-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
    }

    /* Caption */
    .lsp-caption {
      font-size: 16px;
      font-weight: 600;
      color: var(--white);
      text-align: center;
      line-height: 1.6;
      max-width: 600px;
      padding: 0 20px;
    }

    .lsp-carousel-track {
      display: flex;
      transition: transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      will-change: transform;
    }

    .lsp-carousel-slide {
      min-width: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .lsp-carousel-slide img {
      max-width: 100%;
      max-height: 500px;
      object-fit: contain;
      border-radius: 8px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
    }

    /* Navigation Arrows */
    .lsp-nav-btn {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      width: 50px;
      height: 50px;
      background: var(--orange-cta);
      border: none;
      border-radius: 4px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      z-index: 10;
    }

    .lsp-nav-btn:hover {
      background: var(--orange-banner);
      transform: translateY(-50%) scale(1.05);
      box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
    }

    .lsp-nav-btn:active {
      transform: translateY(-50%) scale(0.98);
    }

    .lsp-nav-btn svg {
      width: 24px;
      height: 24px;
      fill: var(--text-dark);
    }

    .lsp-prev { left: 15px; }
    .lsp-next { right: 15px; }

    /* Dots Indicator */
    .lsp-dots {
      display: flex;
      justify-content: center;
      gap: 12px;
      margin-top: 24px;
      position: relative;
      z-index: 1;
    }

    .lsp-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      border: 2px solid transparent;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .lsp-dot:hover {
      background: rgba(255, 255, 255, 0.5);
    }

    .lsp-dot.active {
      background: var(--orange-cta);
      border-color: var(--yellow-highlight);
      transform: scale(1.2);
    }

    /* Slide Counter */
    .lsp-counter {
      text-align: center;
      margin-top: 16px;
      font-size: 14px;
      font-weight: 600;
      color: var(--white);
      text-transform: uppercase;
      letter-spacing: 2px;
      position: relative;
      z-index: 1;
    }

    .lsp-counter span {
      color: var(--orange-cta);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .lsp-carousel-wrapper {
        padding: 30px 15px;
      }

      .lsp-headline {
        font-size: 28px;
        margin-bottom: 24px;
      }

      .lsp-nav-btn {
        width: 40px;
        height: 40px;
      }

      .lsp-nav-btn svg {
        width: 20px;
        height: 20px;
      }

      .lsp-prev { left: 10px; }
      .lsp-next { right: 10px; }

      .lsp-carousel-slide img {
        max-height: 350px;
      }
    }

    @media (max-width: 480px) {
      .lsp-headline {
        font-size: 22px;
        margin-bottom: 20px;
      }

      .lsp-caption {
        font-size: 14px;
      }

      .lsp-nav-btn {
        width: 36px;
        height: 36px;
      }

      .lsp-prev { left: 5px; }
      .lsp-next { right: 5px; }

      .lsp-carousel-slide {
        padding: 10px;
      }

      .lsp-carousel-slide img {
        max-height: 280px;
      }

      .lsp-dots {
        gap: 8px;
      }

      .lsp-dot {
        width: 10px;
        height: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="lsp-carousel-wrapper">
    <!-- Floating Particles -->
    <div class="lsp-particles">
      <div class="lsp-particle"></div>
      <div class="lsp-particle"></div>
      <div class="lsp-particle"></div>
      <div class="lsp-particle"></div>
      <div class="lsp-particle"></div>
      <div class="lsp-particle"></div>
    </div>

    <!-- Headline -->
    <h2 class="lsp-headline">Esempi di potenziali libri full content</h2>

    <!-- Carousel -->
    <div class="lsp-carousel">
      <div class="lsp-carousel-track" id="lspTrack">
        <div class="lsp-carousel-slide">
          <div class="lsp-slide-content">
            <img src="https://i.imgur.com/kzv80Zx.jpeg" alt="Slide 1">
            <p class="lsp-caption">Didascalia immagine 1</p>
          </div>
        </div>
        <div class="lsp-carousel-slide">
          <div class="lsp-slide-content">
            <img src="https://i.imgur.com/wppwdQa.jpeg" alt="Slide 2">
            <p class="lsp-caption">Didascalia immagine 2</p>
          </div>
        </div>
        <div class="lsp-carousel-slide">
          <div class="lsp-slide-content">
            <img src="https://i.imgur.com/Ss1UpwL.jpeg" alt="Slide 3">
            <p class="lsp-caption">Didascalia immagine 3</p>
          </div>
        </div>
        <div class="lsp-carousel-slide">
          <div class="lsp-slide-content">
            <img src="https://i.imgur.com/voDBKDo.jpeg" alt="Slide 4">
            <p class="lsp-caption">Didascalia immagine 4</p>
          </div>
        </div>
        <div class="lsp-carousel-slide">
          <div class="lsp-slide-content">
            <img src="https://i.imgur.com/R7i0aEL.jpeg" alt="Slide 5">
            <p class="lsp-caption">Didascalia immagine 5</p>
          </div>
        </div>
      </div>

      <!-- Navigation Arrows -->
      <button class="lsp-nav-btn lsp-prev" id="lspPrev" aria-label="Previous slide">
        <svg viewBox="0 0 24 24"><path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/></svg>
      </button>
      <button class="lsp-nav-btn lsp-next" id="lspNext" aria-label="Next slide">
        <svg viewBox="0 0 24 24"><path d="M8.59 16.59L10 18l6-6-6-6-1.41 1.41L13.17 12z"/></svg>
      </button>
    </div>

    <!-- Dots -->
    <div class="lsp-dots" id="lspDots"></div>

    <!-- Counter -->
    <div class="lsp-counter">
      <span id="lspCurrent">1</span> / <span id="lspTotal">5</span>
    </div>
  </div>

  <script>
    (function() {
      const track = document.getElementById('lspTrack');
      const slides = track.querySelectorAll('.lsp-carousel-slide');
      const prevBtn = document.getElementById('lspPrev');
      const nextBtn = document.getElementById('lspNext');
      const dotsContainer = document.getElementById('lspDots');
      const currentEl = document.getElementById('lspCurrent');
      const totalEl = document.getElementById('lspTotal');

      let currentIndex = 0;
      const totalSlides = slides.length;

      // Set total
      totalEl.textContent = totalSlides;

      // Create dots
      slides.forEach((_, i) => {
        const dot = document.createElement('button');
        dot.className = 'lsp-dot' + (i === 0 ? ' active' : '');
        dot.setAttribute('aria-label', `Go to slide ${i + 1}`);
        dot.addEventListener('click', () => goToSlide(i));
        dotsContainer.appendChild(dot);
      });

      const dots = dotsContainer.querySelectorAll('.lsp-dot');

      function updateCarousel() {
        track.style.transform = `translateX(-${currentIndex * 100}%)`;
        dots.forEach((dot, i) => {
          dot.classList.toggle('active', i === currentIndex);
        });
        currentEl.textContent = currentIndex + 1;
      }

      function goToSlide(index) {
        currentIndex = index;
        updateCarousel();
      }

      function nextSlide() {
        currentIndex = (currentIndex + 1) % totalSlides;
        updateCarousel();
      }

      function prevSlide() {
        currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        updateCarousel();
      }

      prevBtn.addEventListener('click', prevSlide);
      nextBtn.addEventListener('click', nextSlide);

      // Keyboard navigation
      document.addEventListener('keydown', (e) => {
        if (e.key === 'ArrowLeft') prevSlide();
        if (e.key === 'ArrowRight') nextSlide();
      });

      // Touch/swipe support
      let touchStartX = 0;
      let touchEndX = 0;

      track.addEventListener('touchstart', (e) => {
        touchStartX = e.changedTouches[0].screenX;
      }, { passive: true });

      track.addEventListener('touchend', (e) => {
        touchEndX = e.changedTouches[0].screenX;
        const diff = touchStartX - touchEndX;
        if (Math.abs(diff) > 50) {
          if (diff > 0) nextSlide();
          else prevSlide();
        }
      }, { passive: true });

      // Auto-play (optional - uncomment to enable)
      // setInterval(nextSlide, 5000);
    })();
  </script>
</body>
</html>
